function takeBaniza() {

    var takeCoeficient  = random(69);
    var takeSum         = takeCoeficient + unsesscessfulStepNumber;

    if(takeSum <= 10) {
        // всичко е точно
    }
    else {
        console.log(`Вие успешно актуализирахте версията на софтуер Doors в момента сте с ${currentSoftwareVersion++}`);
        // правим опит за актуализация
    }
}